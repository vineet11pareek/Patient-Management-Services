{{- if .Values.deps.kafka.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka
  namespace: {{.Values.namespace}}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka
  template:
    metadata:
      labels:
        app: kafka
    spec:
      containers:
        - name: kafka
          image: {{.Values.deps.kafka.image}}
          env:
            - name: KAFKA_NODE_ID
              value: "1"
            - name: KAFKA_PROCESS_ROLES
              value: "broker,controller"
            - name: KAFKA_CONTROLLER_QUORUM_VOTERS
              value: "1@kafka:9093"
            - name: KAFKA_LISTENERS
              value: "PLAINTEXT://:9092,CONTROLLER://:9093"
            - name: KAFKA_ADVERTISED_LISTENERS
              value: "PLAINTEXT://kafka:9092"
            - name: KAFKA_CONTROLLER_LISTENER_NAMES
              value: "CONTROLLER"
            - name: KAFKA_LISTENER_SECURITY_PROTOCOL_MAP
              value: "CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT"
            - name: KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR
              value: "1"
          ports:
            - containerPort: {{.Values.deps.kafka.clientPort}}
            - containerPort: {{.Values.deps.kafka.controllerPort}}
---
apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: {{.Values.namespace}}
spec:
  selector:
    app: kafka
  ports:
    - name: kafka
      port: {{.Values.deps.kafka.clientPort}}
      targetPort: {{.Values.deps.kafka.clientPort}}
    - name: controller
      port: {{.Values.deps.kafka.controllerPort}}
      targetPort: {{.Values.deps.kafka.controllerPort}}
  {{end}}